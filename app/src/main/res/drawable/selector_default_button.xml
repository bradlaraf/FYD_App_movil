<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <layer-list>
            <!-- Fondo del botón cuando está seleccionado -->
            <item android:top="-2dp" android:left="-2dp" android:right="-2dp">
                <shape>
                    <solid android:color="@color/color_lightblue_2" /> <!-- Cambia el color del borde inferior -->
                </shape>
            </item>
            <!-- Fondo original del botón -->
            <item>
                <shape>
                    <solid android:color="@color/color_lightblue_2" /> <!-- Cambia el color de fondo del botón -->
                </shape>
            </item>
        </layer-list>
    </item>
    <item>
        <shape>
            <solid android:color="@color/color_massive_deep_blue_v2" /> <!-- Cambia el color de fondo del botón cuando no está seleccionado -->
        </shape>
    </item>
</selector>